mixin tourItem(image, price, title, review, reviewCount, duration, tourPrice)
    .tour
        .tour-top
            img.tour-image(src= image, alt='this-is-image')
            // src để dẫn hình ảnh, nếu hình ảnh lỗi hoặc sai đường dẫn thì hiện alt
            span.tour-price= price
        .tour-content
            h3.tour-tittle=title
            .tour-rating
                .tour-reviews
                    .tour-star
                        img(src='./images/Shape.png', alt='tour-rating')
                    .tour-review-count
                    span= review
                        |  (#{reviewCount} reviews)<!--dấu | nghĩa là chỉ định ở đây là một đoạn chữ-->
                span.tour-duration Duration: #{duration}
            a.tour-button(href='https://act.hoyolab.com/bbs/event/signin/zzz/e202406031448091.html?act_id=e202406031448091&hyl_auth_required=true&hyl_presentation_style=fullscreen&utm_campaign=checkin&utm_id=8&utm_medium=tools&utm_source=hoyolab&lang=vi-vn&bbs_theme=dark&bbs_theme_device=1', target='_blank', rel='noopener noreferrer') Tours Price $#{tourPrice}
